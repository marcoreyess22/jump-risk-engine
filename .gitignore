__pycache__/
*.pyc
.venv/
.DS_Store
.impeccable/

# Salida derivada: la regenera `python -m src.backtest` (~8 min).
# Son 15 MB y no aportan nada que el código no reproduzca.
data/walkforward.csv

# Estado de la corrida diaria: local a cada instalación.
data/estado_diario.json

# Meta del tooling, ajeno al proyecto.
skill-observations/

# Caché del motor diario: datos frescos que NO deben tocar el ancla
# de reproducibilidad versionada en data/prices.csv.
data/prices_live.csv

# Lockfile de uv; el proyecto declara pip/venv como vía soportada.
uv.lock
